services:
  postgres:
    image: postgres:16
    container_name: echo-postgres
    restart: unless-stopped
    ports:
      - "127.0.0.1:5432:5432"
    env_file:
      - .env.docker
    volumes:
      - echo-postgres-data:/var/lib/postgresql/data

volumes:
  echo-postgres-data:
